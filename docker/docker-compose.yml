version: "3.8"

services:
  spw:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: server-power-watchdog
    restart: unless-stopped
    privileged: true
    environment:
      - TZ=America/Toronto
      - WEB_PORT=8080
    volumes:
      - ../data:/config/data
      - ../docker/nut:/etc/nut
      - /dev/bus/usb:/dev/bus/usb
    ports:
      - "80:80"
      - "3493:3493"
